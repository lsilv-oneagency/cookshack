import type { Metadata } from "next";
import MarketingPlaceholder from "@/components/MarketingPlaceholder";

export const metadata: Metadata = {
  title: "Video Support",
  description: "Cookshack video support and how-to content.",
};

export default function VideoSupportPage() {
  return <MarketingPlaceholder title="Video Support" />;
}
